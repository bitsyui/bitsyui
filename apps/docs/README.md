# @bitsyui/docs

Astro documentation site for the Bitsy UI system.

Routes:

- `/`: homepage and section index
- `/system`: system architecture and package boundaries
- `/tokens`: token language and theme model
- `/components`: component guidance and quality bar
- `/templates`: page composition guidance

Run locally with:

```bash
pnpm --filter @bitsyui/docs dev
```
